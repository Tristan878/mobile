<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mobile2.Pages.ListePokke">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Ici notre liste de Pokemon !!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" TextColor="Black"/>



            <CollectionView
                ItemsSource="{Binding PokemonList}"
                SelectionMode="Single"
                SelectionChanged="OnCollectionViewSelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        
                            <StackLayout Orientation="Vertical">
                            <Label>
                            <Label.FormattedText>
                                <FormattedString>
                                        <Span Text="Pokédex : " FontAttributes="Bold" TextColor="Black"/>
                                        <Span Text="{Binding Id}" TextColor="Black"/>
                                </FormattedString>
                            </Label.FormattedText>
                            </Label>

                            <Label>
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Nom : " FontAttributes="Bold" TextColor="Black"/>
                                        <Span Text="{Binding Nom}" TextColor="Black"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                           

                            <Image 
                                Source="{Binding Pictures}" WidthRequest="100" HeightRequest="100" />
                        </StackLayout>
                        
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>



            <!--<ListView
                 CachingStrategy="RecycleElement"
                 ItemsSource="{Binding PokemonList}"
                 RowHeight="70">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label
                                    Text="{Binding Poke}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>